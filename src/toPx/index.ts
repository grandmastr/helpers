/**
 * Converts a number value to a string representation in pixels.
 *
 * @param value - The number value to convert.
 * @returns The string representation of the value in pixels.
 */
const toPx = (value: number = 0): string => {
  return `${value}px`;
};

export default toPx;
