/**
 * Checks if the given object is empty.
 *
 * @param {object} data - The object to check.
 * @return {boolean} - True if the object is empty, false otherwise.
 */
declare const index: (data: object) => boolean;
export default index;
